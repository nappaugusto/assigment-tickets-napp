services:
  app:
    build: .
    ports:
      - "80:80"
    env_file:
      - .env
    volumes:
      # Persist SQLite database outside the container
      - ./data:/app/backend/data
    restart: unless-stopped
