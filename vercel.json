{
  "buildCommand": "cd frontend && npx pnpm@10.29.3 install --frozen-lockfile && npx pnpm@10.29.3 run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
